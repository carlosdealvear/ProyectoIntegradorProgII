<!DOCTYPE html>
<html lang="en">
	<%-include("./partials/header")%>
    <main>
        <div class="uk-section uk-section-xsmall">
            <div class="uk-container uk-container-large uk-flex uk-flex-middle uk-flex-center">
                <section class="uk-flex uk-width-2-3 detalle-container">
                    <% if(locals.user && user.id == post.creador.id) {%>

                        <form action="/post/detallePost/id/<%= post.id%>/eliminar" method="post"> <button type="submit"><a>Eliminar</a></button></form>

                    <% } %>
                    <% if(locals.user && user.id == post.creador.id) {%>

                    <a href="/post/editarPost/id/<%= post.id %>">Editar</a>

                    <% } %>
                    <article class="uk-width-2-3 uk-flex uk-flex-center uk-flex-middle detalle-post">
                        <img src="/fotosPosts/<%= post.img %> " alt="Imagen del post" class="imagen-post ">
                    </article>
                    <article class="uk-width-1-3 uk-flex uk-flex-column comentarios-detalle">
                        <div class="info-post uk-flex uk-flex-middle">
                            <h3><a href="/users/detalleUsuario/<%= post.creador.id %> "><%= post.creador.username %> </a></h3>
                        </div>
                        <div class="lista-comentarios" >
                            <% for(i=0;i<comentarios.length; i++){ %>
                              
                            <div class="comentario uk-flex uk-flex-middle uk-margin-small-top">
                                <div class="uk-width-1-4 uk-flex uk-flex-center">
                                    <img src="https://via.placeholder.com/35" alt="Foto de usuario">
                                </div>
                                <h6 class="uk-margin-remove uk-width-2-4"><a href="/users/detalleUsuario/<%= comentarios[i].usuario.id %> "><%= comentarios[i].usuario.username %> </a> <%= comentarios[i].texto %></h6> 
                                <% if(locals.user && user.id == comentarios[i].usuario.id) {%>

                                    <form action="/post/detallePost/id/<%= post.id%>/borrar/<%= comentarios[i].id %>" method="post"> <button type="submit"><a>Borrar Comentario</a></button></form>
            
                                <% } %>
                                <a href="" uk-icon="icon:heart; ratio: 0.8" class="uk-width-1-4 uk-text-right icono-meGusta"></a>
                            </div>
                           <% }%> 
                           
                           
                        </div>
                        <div class="agregar-comentario uk-flex uk-flex-middle">
                            <form action="" class="uk-text-center" method="post">
                              <input name="text" id="text" type="text" class="uk-input" placeholder="Agregar comentario"> 
                              <% if(locals.user) {%>
                              <button class="uk-button uk-button-primary uk-margin-small-top" type="submit">Comentar</button>
                              <% } %>
                            </form>
                        </div>
                    </article>
                </section>
            </div>
        </div>
    </main>


</body>
<%-include("./partials/footers")%>
</html>